<div class="container-fluid" id="indexContainer">
  {{#if messages.success}}
    <div role="alert" class="alert alert-success">
      {{#each messages.success}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
  {{/if}}
  {{#if messages.error}}
    <div role="alert" class="alert alert-danger">
      {{#each messages.error}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
  {{/if}}
  {{#if messages.info}}
    <div role="alert" class="alert alert-info">
      {{#each messages.info}}
        <div>{{msg}}</div>
      {{/each}}
    </div>
{{!--   {{/if}}
  <div class="row">
    <div class="col-sm-6">
      <ul class="nav navbar-nav" id="signIn">
    {{#if user}}
      <li class="dropdown">
        <a href="#" data-toggle="dropdown" class="navbar-avatar dropdown-toggle">
          {{#if user.picture}}
            <img src="{{user.picture}}">
          {{else}}
            <img src="{{user.gravatar}}">
          {{/if}}
          {{#if user.name}}
            {{user.name}}
          {{else}}
            {{user.email}}
          {{/if}}
          <i class="caret"></i>
        </a>
        <ul class="dropdown-menu">
          <li><a href="/account">My Account</a></li>
          <li class="divider"></li>
          <li><a href="/logout">Logout</a></li>
        </ul>
      </li>
    {{else}} --}}
     {{!--  <li class="{{#ifeq title 'Log in'}}active{{/ifeq}}"><a href="/login">Log in</a></li>
      <li class="{{#ifeq title 'Sign up'}}active{{/ifeq}}"><a href="/signup">Sign up</a></li> --}}
    {{!-- {{/if}}
    </ul>
    </div> --}}
    </div>
    <br>
    <div class="row">
      <div class="col-sm-4" id="landingWelcome">
        <div class="panel" id="landingPanel">
          <div class="panel-body" id="modal">
            <h3>Welcome to Crappy Birdz</h3>
            <p>Step 1: Sign up or Log in 
            <p>Step 2: Click Join Game and start crapping!
            <br>
            <br>
            <a href="/game" role="button" class="btn btn-default">Join Game</a>
          </div>
        </div>
      </div>
      {{!-- <div class="col-sm-4">
        <div class="panel">
          <div class="panel-body">
            <h3>Game 3</h3>
              <div>
                <img src="../public/img/indexImage.jpeg" alt="image">
              </div>
            <a href="#" role="button" class="btn btn-default">Join Game</a>
          </div>
        </div>
      </div> --}}
  </div>
  <br>
  {{!-- <div class="row">
      <div class="col-sm-4">
        <div class="panel">
          <div class="panel-body">
            <h3>Game 2</h3>
            
            <a href="#" role="button" class="btn btn-default">Join Game</a>
          </div>
        </div>
      </div>
      <div class="col-sm-4">
        <div class="panel">
          <div class="panel-body">
            <h3>Game 3</h3>
            
            <a href="#" role="button" class="btn btn-default">Join Game</a>
          </div>
        </div>
      </div>
  </div> --}}
  <br>
</div>
